<Window x:Class="TestEnumRadio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:TestEnumRadio"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow" Width="800" Height="150"
        d:DataContext="{d:DesignInstance Type={x:Type local:MainWindowViewModel},
                                         IsDesignTimeCreatable=True}"
        SizeToContent="Height"
        mc:Ignorable="d">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <UniformGrid Height="Auto" Columns="3">
            <RadioButton Content="1" IsChecked="{Binding Path=Number, Converter={local:SmartConverterEnum}, ConverterParameter={x:Static local:Numbers.Uno}}" />
            <RadioButton Content="2" IsChecked="{Binding Path=Number, Converter={local:SmartConverterEnum}, ConverterParameter={x:Static local:Numbers.Due}}" />

            <RadioButton Content="3" />
        </UniformGrid>
        <Button Grid.Row="1" Command="{Binding ShowValueCmd}" Content="Button" />
    </Grid>
</Window>
